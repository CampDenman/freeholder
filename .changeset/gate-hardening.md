---
"freeholder": patch
---

The machine gates now cover what they claimed to. A new colour gate bans Tailwind's raw palette and arbitrary colour values outside the token system across app, src and plugins — the one non-negotiable that had no lint rule. The money gate's division rule finally matches the codebase's own `*Minor` naming and now reaches server actions in `app/**/*.ts` and `plugins/**`. The merge-completeness test additionally scans for columns merely *named* like contact references, so a `contact_id` without a declared foreign key can no longer slip past, with deliberate exclusions documented in place. The licence gate records its Markdown exemption as a decision instead of skipping prose silently.
