---
"freeholder": minor
---

Traffic figures now tell people apart from crawlers properly — using a
maintained list of known bots plus the shape of each request, which catches
scrapers that copy a real browser's name. Crawler visits are still recorded, so
you can switch between "people" and "people and programs" on the traffic screen
and see how much was set aside either way.
