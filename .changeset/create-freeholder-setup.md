---
"freeholder": minor
"create-freeholder": minor
---

Creating a Freeholder project now checks the environment, can install
dependencies and run migrations, and prints the setup URL — or the exact
recovery if something is still missing.
