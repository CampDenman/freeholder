---
"freeholder": patch
---

The platform now boots: server-rendered site shell on Next.js with strict
TypeScript, the Zod-validated environment and instance config
(`freeholder.config.ts`), the Postgres/Drizzle data layer, and CI quality
gates (lint with architecture boundaries, typecheck, tests, build, and the
changelog gate that produced this very note).
