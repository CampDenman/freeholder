---
"freeholder": minor
---

Pages and chrome can hold an experiment with weighted variants. The server picks one variant per visitor and renders only that HTML — no client-side swap. The same visitor always sees the same variant. Crawlers get the control.
