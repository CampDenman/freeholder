---
"freeholder": minor
---

Your site is now built from blocks stored in the database: pages, and the
header and footer around them, are content you edit rather than code you
deploy. Changes are live on the next page load, every save keeps the previous
version, and FAQ blocks publish their own search-engine markup.
