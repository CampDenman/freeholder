---
"freeholder": minor
---

The live contract now describes errors, auth, webhook envelopes and a version. Agents discover only tools they can call, plus resources and prompts from the same registry. The SDK is a real HTTP client. Webhook deliveries can be inspected (secrets redacted), replayed, and pointed at a new URL.
