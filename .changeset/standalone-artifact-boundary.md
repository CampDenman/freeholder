---
"freeholder": patch
---

Keep runtime filesystem paths from expanding the standalone server trace, remove build-host environment files and source-only repository roots, and fail builds if either reappears.
