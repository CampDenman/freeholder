---
"freeholder": patch
---

Freeholder is now published as a ready-built container image, so deploying it
no longer means building it yourself. Your server pulls the image and starts —
no GitHub account, no build tooling, and no fork of this project required.
