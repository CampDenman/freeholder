---
"freeholder": patch
---

Selected options on two-choice controls (units, and similar settings) now use
the high-contrast colour meant for text on the accent fill, instead of muted
grey that failed WCAG AA on the accent in light mode (C11.12).
