---
"freeholder": patch
---

Allow same-origin camera, microphone, and screen capture so Record Studio can
request the browser permissions it needs. Geolocation and Payment Request stay
disabled, and every capture still requires the browser's explicit user grant.
