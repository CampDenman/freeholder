---
"freeholder": patch
---

Translation drafts are honest about whether a machine ran. When no AI adapter is configured — including a configured provider with no registered implementation — the identity seed is recorded as `draft`, not `machine`, so the translations screen never shows a finished machine translation over untranslated text. A machine status now means a translation model actually produced the values.
