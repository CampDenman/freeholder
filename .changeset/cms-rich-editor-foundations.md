---
"freeholder": minor
---

Body copy is now typed rich text — bold, italic, code, links and lists —
instead of a plain paragraph. Saving HTML in that field is refused. The
editor can insert blocks from a slash menu, undo and redo, duplicate or
copy them, and you can drop in a video or a carefully scoped HTML block.
