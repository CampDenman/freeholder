---
"freeholder": minor
---

Every completion claim now faces all twelve F-criteria at once.
`deploy/f-criteria-matrix.md` audits F01–F12 across 286 rows — every live
§43 checklist item (computed from `scripts/plan-gate.mjs`, never retyped),
the seven mobile items deferred to v2, all seven workspace packages and all
six first-party plugins — with a repository citation or a specific
`N/A — reason` in every cell. `scripts/f-matrix.mjs` and
`tests/core/f-matrix.test.ts` keep the matrix honest the way plan-gate keeps
the checklist honest: a row that drifts from the plan, an empty cell, a
citation that does not resolve, a cited test file outside the suite, or a
lazy "N/A — not applicable" fails `pnpm gates`. The C0.11 audit behind it
re-read the audit-repaired evidence assertion-by-assertion and narrowed 45
stamped coverage claims to what the cited files actually prove; the
annotation rewrites are queued on C11.15. No product behaviour changes.
