---
"freeholder": patch
---

Every change to Freeholder is now checked by crawling a complete example site
the way a search engine would — page titles, descriptions, canonical links,
headings, image descriptions, structured data and the links between pages — and
by an accessibility pass over the main page templates. A change that would have
quietly damaged how your site is found or read now fails before it ships.
