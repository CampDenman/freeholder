---
"freeholder": minor
---

Third-party ad tags are supported and off by default. An HTML tag or a
named network creative only runs when the slot allows it and the visitor
has granted a separate consent cookie. /ads.txt and /app-ads.txt are
generated from the authorized-seller list the owner edits, so the file
crawlers fetch cannot drift from what is actually authorized.
