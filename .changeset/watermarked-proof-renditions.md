---
"freeholder": minor
---

Watermark proof images. Every uploaded photograph now also gets a marked
rendition — the brand logo when one is set, the business name when it is not —
so a client gallery can show proofs without handing over usable files. Marked
renditions never appear on public pages.
