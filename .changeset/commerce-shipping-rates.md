---
"freeholder": minor
---

Shipping now quotes a real rate instead of a single field. You define zones
(most specific match wins), methods (flat, weight, price, item, dimensional,
free over a threshold, pickup, local delivery), rate bands and boxes.
`/admin/shipping` is the owner workspace. Live carrier rates still wait on
an adapter.
