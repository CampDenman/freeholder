---
"freeholder": minor
"@freeholder/mobile-app": minor
---

Owner companion capture can now queue files on the phone while offline: consent,
destination, progress, pause, resume, cancel and retry are visible on the
device, and the batch uploads through the same media pipeline as
`/capture/[token]` once a connection is back.
