# Render (Tier 1)

Web service from the published image, Render Postgres, S3-compatible storage.

## Install

Create a web service pointing at `ghcr.io/campdenman/freeholder:edge`, attach
Postgres, set `.env.example`, deploy, open `/setup`.
