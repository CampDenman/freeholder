# Verify: Replit

- [ ] `/setup` appears on a fresh instance.
- [ ] `GET /api/health` returns `ok` and a `version`.
- [ ] Doctor is green for storage, database and session secret.
- [ ] An upload lands in Replit Object Storage, not the workspace disk.
- [ ] `/admin` is `noindex`.
