# seed/ — the demo business

"Aurora Coast Photography" — full fake data so a fresh deploy is instantly
explorable and CI gates have a realistic site to crawl. Every module
contributes seed data via its manifest (`MASTER.md` §11). One-click purge
from admin.
