# scripts/ — operational entry points

Per `MASTER.md` §10/§14/§23: `setup`, `seed`, `doctor` (plain-English env +
adapter checks), `export` (the ownership button), `migrate` (cross-platform
moves), `backup`.
