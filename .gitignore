# dependencies
node_modules/
.pnpm-store/

# builds
dist/
build/
.next/
out/
coverage/

# environment & secrets
.env
.env.*
!.env.example

# local media, written by the dev-only storage adapter (§18: production media
# lives in object storage, never on instance disk — this directory only ever
# exists on a developer's machine and in test runs)
.data/

# logs & OS noise
*.log
.DS_Store
Thumbs.db

# editors
.idea/
.vscode/
*.swp

# typescript / next
*.tsbuildinfo
next-env.d.ts
