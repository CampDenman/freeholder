# create-freeholder (Apache-2.0)

The deploy bootstrapper: `npx create-freeholder my-business` — target picker
(recipes labeled by support tier), business preset, then a printed walkthrough
with environment checks and recovery steps. Spec: `MASTER.md` §22, C3.14.
