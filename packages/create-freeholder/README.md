# create-freeholder (MIT)

The deploy bootstrapper: `npx create-freeholder my-business` — target picker
(recipes labeled by support tier), business preset, country defaults, then a
printed walkthrough with your values filled in. Spec: `MASTER.md` §22.
Scaffold only for now.
