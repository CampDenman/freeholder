{
  "name": "@freeholder/web",
  "private": true,
  "version": "0.0.0",
  "description": "Freeholder core application — website, admin, CRM spine, commerce, booking, galleries, mail, analytics, MCP server",
  "license": "AGPL-3.0-only",
  "author": "Camp Denman Society",
  "scripts": {
    "build": "echo 'core scaffold — build lands with the v1 slice'",
    "lint": "echo 'core scaffold — lint lands with the v1 slice'",
    "test": "echo 'core scaffold — tests land with the v1 slice'"
  }
}
