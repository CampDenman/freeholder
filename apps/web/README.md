# @freeholder/web

The Freeholder core application (AGPL-3.0-only): the deployable monolith —
public site + CMS, admin, unified CRM spine, commerce, quotes/invoices,
booking, galleries, client portal, mail, first-party analytics, and the MCP
server.

Scaffold only for now. The build contract is defined in the repository's
`MASTER.md`.
